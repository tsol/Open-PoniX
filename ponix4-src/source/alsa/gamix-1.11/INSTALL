Installing gamix

To install gamix, simply run the following commands:

./configure
make
make install

You may need to do an 'su' before the last of these if 
you are not allready root.

(If the configure script is not present, you will have to
run aclocal; automake; autoconf; to generate it.)
