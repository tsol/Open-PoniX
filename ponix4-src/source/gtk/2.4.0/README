последовательность сборки:

glib
atk
freetype
fontconfig
libXft (shared, compiled with Xorg)
pango
libpng (shared, last version)
gtk (для успешной сборки необходимо подправить файл
modules/input/Makefile удалив в нем из строки install: что-то там locale-..
"locale-..") 