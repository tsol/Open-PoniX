# For details on clock and tooltip format see strftime's manpage (man strftime)

# Time in 24-hour notation (%H:%M)
Clock::%R

# Time in 12-hour notation + am/pm string
#Clock::%I:%M %P

# Weekday name + date
Tooltip::%A %x

# shell command to run upon mouse click.
# do not forget '&' (!!!) at the end, otherwise fbpanel will freeze 
# until the command exit
Action::ical &
