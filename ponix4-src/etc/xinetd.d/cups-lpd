service printer
{
	disable = yes
	socket_type = stream
	protocol = tcp
	wait = no
	user = lp
	group = nobody
	passenv =
	server = /cups/lib/cups/daemon/cups-lpd
	server_args = -o document-format=application/octet-stream
}
