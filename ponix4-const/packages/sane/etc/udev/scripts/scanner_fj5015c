#!/bin/sh

BACKEND=avision
NAME=fj5015c

# Ex: DEVICE=/proc/bus/usb/001/019
PORT_ID=$(echo $DEVICE | cut -c15-17)
DEV_ID=$(echo $DEVICE | cut -c19-22)

ALIASFILE=/etc/sane.d/dll.aliases
echo "alias $NAME $BACKEND:libusb:$PORT_ID:$DEV_ID" > $ALIASFILE

