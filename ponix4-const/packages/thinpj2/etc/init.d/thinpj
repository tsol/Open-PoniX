#!/bin/sh

. $TS_GLOBAL

case "$1" in  
init)
    if ! pkg_initialized $PACKAGE; then

	(nohup /bin/safe_thinpj >> /var/log/thinpj.log) &

	pkg_set_init_flag $PACKAGE
    fi
    ;;
stop)
#    kill -SIGUSR2 `pidof thinpj`
    ;;
help)
    echo "Usage: $0 init stop"
    ;;
*)
    exit 1
    ;;
esac

exit 0
