#!/bin/sh

. $TS_GLOBAL

BIN=/bin/skype

SKYPE_LOGIN=$1
SKYPE_PASSWORD=$2

if [ -n "$SKYPE_LOGIN" ] && [ -n "$SKYPE_PASSWORD" ] ; then
        echo $SKYPE_LOGIN $SKYPE_PASSWORD | $BIN --pipelogin  >> $LOGFILE 2>&1
else
        $BIN >> $LOGFILE 2>&1
fi
