#!/bin/sh

echo "
This is a list of USB devices connected to your thin client. Please remember the id 
numbers of the devices you need to share, and then specify them in your ponix configuration
files as following:

USBREDIR_DEV_X=\"ID\"

Where X is number of config variable starting with 1, and ID is the id of
the usb device in the list below.

For example:

USBREDIR_DEV_1=\"3\"

=====================================================================
" > /tmp/usbredir.txt

usbsrv -l | grep -v "===" >> /tmp/usbredir.txt
xdmenu_edit /tmp/usbredir.txt 128 44
rm /tmp/usbredir.txt

