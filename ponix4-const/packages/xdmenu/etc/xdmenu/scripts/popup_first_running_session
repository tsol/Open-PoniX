#!/bin/sh

. $TS_GLOBAL

let i=0
while [ $i -le $MAX_SESSIONS ] ; 
do
	SESSION_TYPE=`eval echo '$SESSION_'$i'_TYPE'`
	if [ -n "$SESSION_TYPE" ] ; then
                if is_session_running $i ; then
			$XDMENU_SCRIPTS/bring_session_to_front $i
			exit                
		fi
	fi
	let i=i+1
done

